import { ResetPasswordForm } from "@/components/auth/password-recovery-forms";
export default async function ResetPasswordPage({ searchParams }: { searchParams: Promise<{ token?: string }> }) { const { token } = await searchParams; return <main className="mx-auto flex min-h-[70vh] max-w-md items-center px-4 py-16"><section className="w-full space-y-6 rounded-3xl border border-[#ddcfbb] bg-[#fffdf9] p-8 text-center shadow-xl"><div><p className="text-xs font-semibold uppercase tracking-[0.3em] text-orange-600">Account recovery</p><h1 className="mt-3 font-serif text-3xl font-semibold">Choose a new password</h1></div><ResetPasswordForm token={token} /></section></main>; }
