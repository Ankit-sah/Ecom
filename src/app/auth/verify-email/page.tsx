import { VerifyEmailForm } from "@/components/auth/password-recovery-forms";
export default async function VerifyEmailPage({ searchParams }: { searchParams: Promise<{ token?: string }> }) { const { token } = await searchParams; return <main className="mx-auto flex min-h-[70vh] max-w-md items-center px-4 py-16"><section className="w-full space-y-6 rounded-3xl border border-[#ddcfbb] bg-[#fffdf9] p-8 text-center shadow-xl"><div><p className="text-xs font-semibold uppercase tracking-[0.3em] text-orange-600">Account security</p><h1 className="mt-3 font-serif text-3xl font-semibold">Verify your email</h1><p className="mt-2 text-sm text-neutral-600">Confirm your address to secure your account.</p></div><VerifyEmailForm token={token} /></section></main>; }
